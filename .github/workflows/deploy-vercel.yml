name: Deploy to Vercel

on:
  push:
    branches:
      - main
      - develop
      - feature/*
      - duykhanh/test-deploy

jobs:
  deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./frontend-next

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Vercel CLI and pnpm
        run: |
          npm install --global vercel@latest
          npm install --global pnpm@9

      - name: Create Vercel project config
        run: |
          mkdir -p .vercel

          cat > .vercel/project.json <<EOF
          {
            "orgId": "${VERCEL_ORG_ID}",
            "projectId": "${VERCEL_PROJECT_ID}"
          }
          EOF

          echo "Created Vercel project config:"
          cat .vercel/project.json

        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Verify Vercel authentication
        run: |
          vercel whoami --token="${VERCEL_TOKEN}"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Pull Vercel Project Settings and Environment
        run: |
          vercel pull \
            --yes \
            --environment=production \
            --token="${VERCEL_TOKEN}"

          echo "Vercel files:"
          find .vercel -maxdepth 2 -type f -print || true

          echo "Environment file:"
          ls -la .env.local || true

        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Check Environment Variables
        run: |
          echo "Loaded environment keys:"
          grep -E "^[A-Z0-9_]+=" .env.local || true

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile

      - name: Build Project Artifacts
        run: |
          vercel build \
            --prod \
            --token="${VERCEL_TOKEN}"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        run: |
          vercel deploy \
            --prebuilt \
            --prod \
            --token="${VERCEL_TOKEN}"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}